# openspec/config.yaml
# Auto-generated by sdd-init
schema: spec-driven

context: |
  Project: calendario-dietas
  Stack: React + Vite + TanStack Query + React Router + Tailwind CSS
  Backend: Supabase (Postgres, Auth, Storage)
  Deployment: GitHub Pages (static SPA export)
  Architecture: SPA with Supabase backend, no SSR
  Testing: None configured yet — to be added
  Language Domain: Technical artifacts default to English; conversational Spanish OK

rules:
  proposal:
    - Include rollback plan for risky changes
    - Define scope boundaries clearly
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Consider Supabase RLS policies and Auth flows
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner: null
  reason: "No test runner configured yet. Set strict_tdd:true after adding vitest or equivalent."
  layers:
    unit: null
    integration: null
    e2e: null
  coverage: false
  quality:
    linter: null
    type_checker: null
    formatter: null

deployment:
  platform: GitHub Pages
  strategy: Static SPA export via Vite
  domain: null # TBD
